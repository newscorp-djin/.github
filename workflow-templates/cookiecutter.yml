name: Z-Cookiecutter

# Multiple trigger types
on:
  workflow_dispatch:
    inputs:
      template:
        type: choice
        description: 'Select template'
        options:
          - java
          - dotnet
          - python
          - golang
          - python-api-docker
          - python-api-pypi
          - groovy
      app_name:
        type: string
        description: 'Enter the name of the application'
  schedule:
    - cron: '0 0 * * *'  # Daily at midnight UTC

jobs:
  # First shared workflow call
  create-workflow:
    uses: newscorp-djin/github-workflows/.github/workflows/cookiecutter.yml@main
    secrets: inherit
