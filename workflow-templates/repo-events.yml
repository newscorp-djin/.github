name: Application Events Workflow

# Multiple trigger types
on:
  push:
  pull_request:
  issue_comment:
  issues:
  pull_request_review:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }} # Unique group for each workflow and branch
  cancel-in-progress: true # Cancel old runs on new push

jobs:
  events-workflow:
    uses: newscorp-djin/github-workflows/.github/workflows/app-skeleton-generator.yml@main
    secrets: inherit